<style>
  .bg { fill: var(--background); } .text { fill: var(--text-colour);
  font-family: Helvetica, sans-serif; dominant-baseline: text-before-edge; }
  .link:hover, .link:focus-visible { text-decoration: underline; } .artwork {
  transform: translate(20px, 10px); } .title { font-size: 14px; font-weight:
  bold; transform: translate(75px, 15px); } .artist { font-size: 12px;
  transform: translate(75px, 33px); } .fav-feat-title { fill: #999; font-size:
  12px; transform: translate(535px, 17px); text-anchor: end; } .fav-feat-track {
  font-size: 12px; transform: translate(535px, 33px); text-anchor: end; }
  .profile { font-size: 18px; transform: translate(580px, 16px); text-anchor:
  end; fill: var(--logo-colour); } .logo-acc { font-size: 16px; transform:
  translate(160px, 18px); fill: var(--logo-colour); } .icon { transform:
  translate(550px, 17px); } .path { stroke: var(--text-colour); stroke-width:
  30px; } .icon svg { transition: fill 300ms; } .icon:hover .heart { fill: pink;
  } .icon:hover .bag { fill: lightblue; } .hr { fill: var(--hr); } #logo {
  transform: translate(20px, 17px); } #history { transform: translate(0, 50px);
  } .err-title { transform: translate(115px, 75px); font-size: 30px; } .err-link
  { fill: #1DA0C3; } .err-body { transform: translate(60px, 115px); }
</style>